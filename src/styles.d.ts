// Ambient declaration (no import/export here, so it stays ambient — not a module augmentation).
// Phosphor web ships CSS-only entry points with no type declarations.
declare module '@phosphor-icons/web/regular';
